#include<iostream>
#include<bits/stdc++.h>
using namespace std;
int main(){
    int n,q;
    cin >>n>>q;
    int tables[n+1],r[n+1];
    memset(tables,-1,sizeof(tables));
    int ma=0;
    for(int i=1;i<=n;i++){
        cin >>r[i];
        if(ma<r[i])
            ma=r[i];
    }
    for(int )
}

